# `internal/adapters/gigachat`

Клиент GigaChat API.

Используется в двух сценариях:
- генерация краткой выжимки по транскрипции;
- ответы на `/chat`.

Также пакет формирует embeddings, которые используются в `/smart_find` и хранятся в БД для семантического поиска.
